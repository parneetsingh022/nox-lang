[package]
name = "nox-lang"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "nox"
path = "src/main.rs"

[profile.release]
lto = true
strip = true
codegen-units = 1

[dependencies]
phf = { version = "0.14.0", features = ["macros"] }
