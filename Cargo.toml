[workspace]
members = [
    ".",
    "crates/nyx-diagnostic",
    "crates/nyx-lexer",
    "crates/nyx-source",
    "crates/nyx-token",
]
resolver = "3"

[workspace.dependencies]
miette = { version = "7.6.0", features = ["fancy"] }
strum = "0.28.0"
strum_macros = "0.28.0"
thiserror = "2.0.18"

insta = "1.48.0"
rstest = "0.26.1"

nyx-diagnostic = { path = "crates/nyx-diagnostic" }
nyx-source = { path = "crates/nyx-source" }
nyx-token = { path = "crates/nyx-token" }
nyx-lexer = { path = "crates/nyx-lexer" }

[package]
name = "nyx-lang"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "nyx"
path = "src/main.rs"

[profile.release]
lto = true
strip = true
codegen-units = 1

[dependencies]
miette.workspace = true
strum.workspace = true
strum_macros.workspace = true
thiserror.workspace = true
nyx-diagnostic.workspace = true
nyx-source.workspace = true
nyx-token.workspace = true
nyx-lexer.workspace = true

[dev-dependencies]
insta.workspace = true
rstest.workspace = true
